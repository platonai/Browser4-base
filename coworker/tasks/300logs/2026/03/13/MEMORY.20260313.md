## Daily Memory - 2026-03-13

- **Corrected Browser4 MCP launcher guidance for the packaged jar:** Investigated a failed `java -cp ... Browser4.jar ai.platon.pulsar.app.Browser4Launcher --app=mcp` invocation and confirmed the packaged `Browser4.jar` is a Spring Boot executable jar, which stores application classes under `BOOT-INF\classes`. **Outcome:** Updated the MCP runner KDoc and `browser4\browser4-agents\README.md` to use the supported `java -jar ... --app=mcp` form and explicitly warn that direct `-cp` launching of `Browser4Launcher` will fail for the packaged jar. **Learning:** When documenting or wiring Browser4 executable jars for MCP clients, use `-jar` instead of `-cp` against `target\Browser4.jar`; direct classpath startup only works with plain jars, not repackaged Boot jars.

- **Validated the fix with module-local builds/tests and launch smoke checks:** Root-reactor Maven commands failed because the repo currently references a missing `browser4\browser4-spa` child module, so validation was rerun with module-local `-f` commands. **Outcome:** `browser4\browser4-agents` compiled successfully, `Browser4LauncherTest` passed, `pulsar-agentic` compiled successfully, and a `java -jar ... --app=mcp` smoke launch completed without the class-not-found failure. **Learning:** In this repo state, prefer module-local Maven invocations for targeted validation when the root reactor is temporarily incomplete.
