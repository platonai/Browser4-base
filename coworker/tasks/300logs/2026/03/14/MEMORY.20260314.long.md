## Daily Memory - 2026-03-14

- **DOM model pipeline clarified:** Added KDoc to `DOMStateBuilder.kt` documenting the flow: `MergedDOMTree` (rich merged DOM/AX/snapshot) -> `TinyDOMTreeNode` (builder-stage pruned/display-aware) -> `MicroDOMTreeNode` (compact agent-facing form). **Learning:** Explain fidelity loss at the compaction boundary, where rich internal nodes become minimal prompt nodes.

- **Focused validation for DOM serialization:** Re-ran `.\mvnw.cmd -f pulsar-core\pulsar-browser\pom.xml -Dtest=DOMStateBuilderTest -D"surefire.failIfNoSpecifiedTests=false" test` before/after related changes; it passed both times. **Learning:** `DOMStateBuilderTest` is the smallest reliable safety check for DOM serialization and aria snapshot work.

- **Nano-tree range pruning fixed:** Expanded `MicroToNanoTreeHelperTest` to cover overlapping leaf inclusion, open-start/closed-end semantics, merged seen-chunk behavior, invalid intervals, and full-range consistency. Root cause: `toNanoTreeInRangeRecursive()` filtered leaf children before recursion, dropping valid prompt nodes. Fix: recurse into in-range children first, then prune only empty placeholder nano nodes. **Learning:** Range tests must include real leaf descendants; pre-recursion filtering can remove the exact compact nodes prompts need.

- **Full-range aligned with unfiltered traversal:** Reviewed `toNanoTree()`, `toNanoTreeInRange()`, and `toNanoTreeUnfiltered()`, then changed `MicroToNanoTreeHelper.toNanoTreeInRange()` so canonical full range (`0.0..1_000_000.0`) delegates to the unfiltered path. This removed drift where range traversal dropped geometry-less nodes, zero-height nodes at `y == 0`, or nodes beyond the sentinel cutoff. **Learning:** If “full range” should equal unfiltered behavior, both must share the same code path.

- **Right focused validation pair identified:** Ran `.\mvnw.cmd -f pulsar-core\pulsar-browser\pom.xml "-Dtest=MicroToNanoTreeHelperTest,DOMStateBuilderTest" -D"surefire.failIfNoSpecifiedTests=false" test`. **Learning:** `MicroToNanoTreeHelperTest` + `DOMStateBuilderTest` is the efficient validation pair for DOM serialization changes; in PowerShell, quote comma-separated Surefire class lists as one `-Dtest=...` value.

- **Direct aria snapshot rendering from enhanced tree:** Added `AriaSnapshotRenderer`, made `DOMState.ariaSnapshot` prefer `EnhancedDOMTreeNode`, extracted shared YAML formatting helpers, and deprecated `AriaSnapshotForNanoDOMTreeRenderer` for compatibility. This preserved AX metadata like description and autocomplete-style/non-default properties lost during enhanced -> micro -> nano compaction. **Learning:** Fidelity-sensitive rendering should happen before a lossy compaction boundary, or retain direct access to the richer tree.

- **Formatting/tooling note:** Validation confirmed description/autocomplete-style AX metadata was preserved. **Learning:** When appending Markdown via PowerShell here-strings, avoid unescaped backticks or use escape-aware construction.

- **LLM-backed proxy test made timeout-skippable:** Updated `UniversalProxyParserTest.kt` so `testParseuniversalproxyWithNonStandardProxy()` catches `ChatModelException` and converts known timeout messages into a JUnit assumption skip, while rethrowing non-timeout failures. **Learning:** Guard only the known remote-timeout path; don’t weaken the whole test.

- **Surefire profile behavior confirmed:** A focused `UniversalProxyParserTest` run executed 0 tests until rerun with `-DrunE2ETests=true`, after which it ran and skipped locally due to missing LLM config. **Learning:** In this repo, `@Tag("RequiresServer")` tests require `-DrunE2ETests=true` even for focused Surefire runs.

- **Coworker memory kept append-only:** Appended daily memory without editing monthly history; verified `coworker\tasks\300logs\2026\03\MEMORY.202603.md` already had the `2026-03-13` rollup. **Learning:** Verify first, then append; never rewrite prior month entries.

- **Kotlin aria snapshot cursor formatting aligned with TypeScript:** Updated `AriaSnapshotFormatting.kt` so `[cursor=pointer]` is emitted only for the highest rendered ref in a pointer-bearing subtree, matching `ariaSnapshot.ts`. Added a regression in `DOMStateBuilderTest.kt` for an interactive container with an interactive child; focused test passed. **Learning:** Matching Playwright aria snapshot output requires recursive formatting state, not just correct node metadata—ancestor-emitted cursor markers must suppress descendant duplicates.


- **Test wrappers aligned with current modules:** Updated `bin/test.ps1`, `bin/test.sh`, and `bin/README.md` to remove the deleted `kotlin-sdk` / `python-sdk` targets, keep `nodejs-sdk` pointed at the surviving `sdks/browser4-cli` package, and emit a clear error when callers request removed SDK test modes. **Learning:** When modules are retired, helper scripts should fail explicitly for stale entrypoints instead of forwarding them as opaque Maven arguments.

- **Root Maven test path hardened:** Both test wrappers now add `-P=-examples` so the active-by-default `examples` profile no longer drags in the removed `browser4/browser4-spa` module during test runs. Validation: `./bin/test.ps1 fast -q -DskipTests` and `./bin/test.ps1 core -q -DskipTests` succeeded; `./bin/test.ps1 nodejs-sdk -- --runInBand` reached `sdks/browser4-cli` and failed only on the existing `tests/commands.test.ts` assertion for missing `commands['list']`; `bash -n ./bin/test.sh` passed after restoring LF line endings. **Learning:** In this repo, script-level validation should separate wrapper/regression issues from pre-existing package-test failures and from Windows Bash CRLF quirks in `mvnw`.
