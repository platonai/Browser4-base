## Daily Memory - 2026-03-09

- **PulsarWebDriver click test realignment:** Replaced the disabled `PulsarWebDriverClickTests` coverage that still targeted the old dynamic page with active E2E tests against `generated/interactive-screens.html`, using current selectors like `#addButton` and `#toggleMessageButton`. The new suite now validates count-based clicks, modifier clicks, no-op missing-element clicks, disabled-button behavior, navigation, and sequential interactions that match the live page structure. **Learning:** When generated mock pages evolve, refresh test fixtures against the real HTML instead of preserving stale selectors; for Browser4 E2E classes tagged `E2ETest`, remember to enable them with `-DrunE2ETests=true` or Maven will report zero executed tests.

- **Validation outcome:** Verified the rewritten class with `./mvnw.cmd -f pulsar-tests\\pom.xml -pl pulsar-it-tests -am -DrunE2ETests=true -Dtest=PulsarWebDriverClickTests -D"surefire.failIfNoSpecifiedTests=false" test`, which finished with 13 tests run, 0 failures, 0 errors. **Learning:** Sequential click tests on this page are more reliable when assertions reflect actual current page behavior (for example, the calculator button can legitimately produce the validation message when fields are not populated in the interaction flow being tested).

- **Coworker periodic-script rename cleanup:** Renamed the legacy periodic coworker entrypoints to clearer queue-oriented names by adding `coworker/scripts/process-coworker-queue.ps1` and `coworker/scripts/process-draft-refinement-queue.ps1`, moving the scheduler-backed implementations to `coworker/scripts/deprecated/process-coworker-queue.ps1` and `coworker/scripts/deprecated/process-draft-refinement-queue.ps1`, and retargeting scheduler/docs accordingly while keeping the old `run_*_periodically.ps1` names as deprecation shims. **Learning:** Once `coworker-scheduler.ps1` owns recurrence, legacy helper names should describe the queue-processing work they perform instead of implying that they are the scheduler; keeping compatibility wrappers lets the repo adopt clearer names without breaking existing automation.

- **Validation outcome:** Verified the renamed coworker scripts by PowerShell-parsing every touched `.ps1` entrypoint/implementation and importing `coworker-scheduler.config.psd1` to confirm the configured script paths resolve successfully. **Learning:** For automation scripts that would cause side effects if executed in a shared environment, AST parsing plus config-path validation is a safe targeted check that still catches broken renames and syntax regressions.

- **ARIA snapshot rendering alignment:** Implemented Playwright-style ARIA snapshot rendering via DOMState.render() and PageHandler.ariaSnapshot(), adding a dedicated renderer that emits roles, names, state attributes, refs, pointer hints, inline text, and /url properties from DOM/AX data. Also added href to the default included attributes so link URLs survive standard DOM-state construction for rendering. **Learning:** For Playwright-compatible accessibility YAML, render from the richer micro/unfiltered tree instead of generic object YAML so refs, cursor hints, and semantic props can be reconstructed deterministically.

- **Validation outcome:** Verified with .\mvnw.cmd -pl pulsar-core\pulsar-browser -am -Dtest=DOMStateBuilderTest -D"surefire.failIfNoSpecifiedTests=false" test, which passed with 11 tests run, 0 failures, 0 errors, 1 skipped after adding focused renderer coverage and default-link-url assertions. **Learning:** A small renderer-focused unit test is enough to lock down Playwright-style formatting without needing a full browser E2E run for every output tweak.
