# Improve rename scripts

Improve rename.ps1, rename.sh, provide a new option to specify a directory, all files in the directory will be checked and renamed if necessary.

The following files should be renamed:

- file name contains only digits, e.g. 1.md, 2.txt, etc.
