# Convert the DOM tree to YAML format

Implement the `toYaml` method in the `DOMSerializer` class to convert the DOM tree into a YAML format.

This will allow for easier readability and manipulation of the DOM structure in a human-friendly format.

The method to implement is as follows:

```kotlin
ai.platon.browser4.driver.chrome.dom.DOMSerializer.toYaml
```
